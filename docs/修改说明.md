# 修改说明

1. 将 `cpp` 中的 <u>PaddleOCR 部分</u> 更新至 [官方示例中的 3.1 版本](https://github.com/PaddlePaddle/PaddleOCR/tree/release/3.1/deploy/cpp_infer) (因为 3.2+ 版本重构了整个项目结构，懒得改了)，包括 `cpp/include`、`cpp/src` 和 `cpp/CMakeLists.txt`。
2. 修改 `cpp\include\ocr_rec.h` 文件，修复了当 rec 模型 <u>含 yml 配置文件</u>、<u>设置了 `rec_char_dict_path`</u> 且 <u>yml 配置文件含有 `character_dict`</u> 时崩溃的问题。
3. 优化了一些 包含文件